name: multistage pipeline
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'building'
  test:
    runs-on: ubunut-latest
    needs: build
    steps:
      - run: echo 'testing'
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: echo 'deployment'


    
