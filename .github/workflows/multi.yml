name: multistage
on:
  push:
    branches:
      - main
jobs:
 build: 
   runs-on: ubuntu-latest
   steps:
   - run: echo" building .."
 test: 
  runs-on: ubuntu-latest
  needs: build
  steps:
  - run: echo" test stage"
 deploy: 
  runs-on: ubuntu-latest
  needs: test
  steps:
    - run: echo" deploy  step"
    
 
